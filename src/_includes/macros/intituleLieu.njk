{% macro intituleLieu(slug, lieux) %}
  {%- if slug -%}
    {%- for lieuItem in lieux -%}
      {%- if lieuItem.fileSlug == slug -%}
        {{ lieuItem.data.title | replace("'", '’') }}{% if lieuItem.data.city %}, à {{ lieuItem.data.city | replace("'", '’') }}{%- endif -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endif -%}
{% endmacro %}
